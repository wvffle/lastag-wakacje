{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "build:cf": "NITRO_PRESET=cloudflare yarn build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "preview:cf": "yarn build:cf && npx miniflare .output/server/index.mjs --site .output/public",
    "deploy": "npx wrangler publish"
  },
  "devDependencies": {
    "nuxt": "3.5.1"
  },
  "dependencies": {
    "@fontsource/modak": "^5.0.1",
    "@fontsource/quicksand": "^5.0.1",
    "@unocss/nuxt": "^0.52.4",
    "aos": "^2.3.4",
    "augmented-ui": "^2.0.0",
    "node-fetch": "^3.3.1",
    "ohmyfetch": "^0.4.21",
    "reseter.css": "^2.0.0",
    "stylus": "^0.59.0"
  }
}
